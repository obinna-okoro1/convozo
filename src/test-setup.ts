/**
 * Test setup file for Karma + Jasmine
 * Loaded before every spec via angular.json setupFiles.
 *
 * This app runs zoneless (no zone.js), so zone.js/testing is not imported.
 * Timer-based tests use jasmine.clock() instead of fakeAsync/tick.
 */

// eslint-disable-next-line @typescript-eslint/triple-slash-reference
/// <reference types="jasmine" />
